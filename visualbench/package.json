{
  "name": "mjj-visualbench",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "uv run --project .. python generate.py",
    "capture": "node capture.mjs",
    "score": "uv run --project .. python score.py",
    "budget": "uv run --project .. python budget_bench.py",
    "test": "node --test test/cases.test.mjs",
    "bench": "npm run generate && npm test && npm run capture && npm run score && npm run budget"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
